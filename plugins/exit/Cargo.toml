[package]
name = "exit"
description = "Exit the shell with an optional status code"
version = "0.1.0"
edition = "2024"
build = "build.rs"

[lib]
crate-type = ["cdylib"]

[dependencies]
abi_stable = "0.11"

rush-plugin = { path = "../../rush-plugin" }
